declarer tab_entier : tableau_de 10 entier;
declarer indice, max : entier;
max <- tab_entier[0];
indice <- 0;
pour (i variant_de 1 a taille(tab_entier)-1)
faire
	si (tab_entier[i] > max)
		max <- tab_entier[i];
		indice <- i;
	fsi
ffaire
afficher(incide);